# Explore_BPX dependencies.
# BPX is pinned exactly: the app is built and tested against this version and
# all BPX coupling is isolated in app/core/bpx_gateway.py.
bpx==1.1.1
PySide6>=6.6
PyYAML>=6.0
# LaTeX rendering of parameter symbols (mathtext, no TeX install needed);
# also the plotting backend for the future Analysis workspace.
matplotlib>=3.8

# Development-only dependencies (tests and lint).
pytest
pytest-qt>=4.5
ruff>=0.16
