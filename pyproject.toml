[project]
name = "explore-bpx"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bpx==1.1.0",
    "matplotlib>=3.8",
    "pyside6>=6.6",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-qt>=4.5",
]
