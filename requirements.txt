# Explore_BPX dependencies.
# BPX is pinned exactly: the app is built and tested against this version and
# all BPX coupling is isolated in app/core/bpx_gateway.py.
bpx==1.1.0
PySide6>=6.6
PyYAML>=6.0
